q*()
q()
q()
library(ggplot2)
library(rjson)
JsonData <- fromJSON(file = "data.json" )
str(JsonData)
str(JsonData.length())
install.packages("jsonlite")
JsonData <- fromJSON(file = "data.json" )
dat <- do.call(rbind, lapply(JsonData$features, function(x) data.frame(x$properties)))
document <- fromJSON(file = "data.json", method = 'C')
dat <- do.call(rbind, lapply(document$features,
function(x) data.frame(x$properties)))
dat <- do.call(rbind, lapply(document$features,
function(x) data.frame(x$properties)))
JsonData <- fromJSON(file = "data.json" )
